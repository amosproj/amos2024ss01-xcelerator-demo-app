# Docs

### Setup

1. Install Prisma
    ```sh
    pnpm install -D prisma
    ```
2. Install the Prisma Client
    ```sh
    pnpm install @prisma/client
    ```
3. Initialize Prisma
    ```sh
    pnpx prisma init
    ```

### Usage
