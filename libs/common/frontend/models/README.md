# common-frontend-models

This library is a collection of models that are used through out by the frontend applications.

## environment

This lib contains the app environment tokens, which are used to inject the environment variables into the application.#
It can be used like this:

```ts
private readonly _environment = inject(APP_CONFIG);
```
